# Runtime state — generated by the API + workers, not source code.
# Production regenerates these on its own; tracking them in git churns
# every commit and overwrites production state on each deploy.
cache/
logs/

# Keep the directory structure so the app doesn't have to mkdir on first
# write — these stub files are tracked, the contents above are not.
!cache/.gitkeep
!logs/.gitkeep
